{
  "name": "lambda-images",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:cron": "node build.js cron",
    "build:spot-task": "node build.js spot-task",
    "check": "biome ci src",
    "test": "vitest",
    "e2e:cron": "node -r ts-node/register --env-file=./.env src/cron/e2e.ts",
    "e2e:spot-task": "node -r ts-node/register --env-file=./.env src/spot-task/e2e.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@sentry/esbuild-plugin": "^2.14.0",
    "@types/aws-lambda": "^8.10.133",
    "esbuild": "^0.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.507.0",
    "@sentry/node": "^7.100.1",
    "valibot": "^0.28.0"
  }
}
